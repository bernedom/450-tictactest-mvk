name: Devcontainer

on:
  push:
    paths:
      - '.devcontainer/Dockerfile'
    branches:
      - 'devcontainer/*'

      

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7

     